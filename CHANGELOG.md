# 更新日志 (Change Log)

本项目的所有主要更新和迭代日志都将记录于此。

---

## [v1.3.0] - 2026-05-31

### 🚀 新增与优化
- **系统日历日程同步**：
  - 引入了系统的 `EventKit` 框架，在设置面板中新增了“同步系统日历日程提醒”的开关复选框。
  - 支持自动请求 macOS 系统日历读取与完整访问权限（Sonoma+ 支持）。
  - 支持后台监听 `EKEventStoreChanged` 系统广播，日程有变动时静默自动更新提醒队列。
  - **日程预览清单**：在开启日历同步时，设置面板内会自动用小蓝色圆点和时间戳列出今天接下来最近的 3 条日程，方便直观验证。
  - **准点飞出提醒**：在日程开始时间的整分秒，小飞机会自动拉起，横幅上显示“日程开始: [日程名称]”并平滑飞过。

---

## [v1.2.0] - 2026-05-31

### 🚀 新增与优化
- **画板加长加大**：将拖拽的提示文字横幅（`.banner-container`）尺寸从大号 `400px * 80px` 进一步加宽加大至 **`520px * 100px`**，适配超长中文文本。
- **平稳滑行（去除震动）**：
  - 彻底移除了飞行大容器（`#flight-group`）上的上下起伏（`bobbing`）动画。
  - 移除了横幅自身的晃动（`bannerWave`）与绳索的摆动（`ropesWiggle`）动画。
  - 画板与飞机现在进行绝对平滑的水平直线位移，彻底解决了高频晃动导致的阅读疲劳问题。
- **字体细节缩减**：将画板内字体从 `22px` 缩减为 **`18px`**，铃铛图标大小缩减为 **`26px`**。大画板衬托精致的小字体，阅读观感更优。
- **零抖动 GPU 渲染**：
  - 弃用 CPU 驱动的 `left` 属性动画，改为使用 **GPU 驱动的 `transform: translate3d` 整体平移**，显著减少重绘（Repaint）。
  - 在大容器上开启 `will-change: transform` 图层缓存。
  - 在 JavaScript 中使用 `Math.round(currentX)` **强制物理像素点对齐**，彻底消除了亚像素运动引起的文字边缘变糊和发抖问题。
- **彻底去除尾气**：完全清除了飞机屁股后的白色粒子烟雾画布与相关逻辑，排除任何字迹遮挡。
- **滑行降速**：整体横穿屏幕的时间从 `10秒` 调慢至 **`16秒`**，滑行过程优雅从容，提升了阅读舒适度。

### 🔧 维护与工程
- **Git & GitHub 托管**：初始化了本地仓库，配置 `.gitignore` 忽略 `.app` 编译包，并通过配置 **SSH Over Port 443** 绕过常规端口限制，顺利将项目代码同步至 GitHub：`git@github.com:micky-zh/alarm-clock.git`。

---

## [v1.1.0] - 2026-05-31

### 🚀 新增与优化
- **WebView 动效迁移**：将原来的 SwiftUI 飞行动画层重构为使用底层的透明 `WKWebView` 网页动画层，解决了原生动画在高分辨率屏幕下的微幅锯齿，带来了 60fps 浏览器级别的动画表现。
- **飞机大改版**：
  - 引进 Base64 编码的 3D 泥塑小飞机美术资产，替换了之前的简笔画风格，极具视觉冲击力。
  - 飞机尺寸比例拉长放大（从最初的 `240x150` 改良至大号 `340x180`）。
  - 新增 3D 小飞机专用的 CSS 旋转螺旋桨 SVG 覆盖层。
- **V 字形护航编队**：在“加急重要”闹钟模式下，主飞机上、下方会自动添加两架小飞机进行护航飞行。
- **烟雾防遮挡尝试**：为网页容器划分了不同的 3D 景深 Z 轴图层（`preserve-3d`），尝试将尾气画布置于文字后方。

---

## [v1.0.0] - 2026-05-31

### 🎉 初始发布
- **闹钟核心逻辑**：实现 macOS 状态栏图标（MenuBar App）的创建与点击交互。
- **设置面板**：提供设置闹钟时间、提示文本、加急开关和悬停/普通模式选择的 SwiftUI 设置界面。
- **初始飞行动画**：小飞机拖拽玻璃拟态横幅从屏幕左侧飞入并横穿屏幕。
- **打包部署**：编写了 `pack.sh` 脚本，可一键将 Swift 项目编译打包为带精美 3D 小飞机软件图标的独立 `.app` 应用程序并发送至桌面。
